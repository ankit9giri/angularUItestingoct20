import { LinkBudgetDirection } from './link-budget-direction';

describe('LinkBudgetDirection', () => {
  it('should create an instance', () => {
    expect(new LinkBudgetDirection()).toBeTruthy();
  });
});
